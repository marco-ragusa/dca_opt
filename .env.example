LOG_LEVEL=INFO

# Cache backend: "local" (in-memory, single process) or "redis" (shared across workers)
CACHE_BACKEND=local
CACHE_TTL_SECONDS=300

# Required when CACHE_BACKEND=redis (no default — startup fails if missing)
# REDIS_URL=redis://localhost:6379/0
